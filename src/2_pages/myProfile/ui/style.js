import styled from "styled-components";

const myProfile = styled.div`
  height: 100%;
  padding: 50px;
  display: flex;
  flex-direction: column;
  gap: 80px;
`;

export const S = {
    myProfile,
};
