import styled from "styled-components";

const BtnContainter = styled.div`
    display: flex;
    width: 210px;
    gap: 20px;
    justify-content: center;
    margin-left: auto;
`;

export const S = {
    BtnContainter,
}