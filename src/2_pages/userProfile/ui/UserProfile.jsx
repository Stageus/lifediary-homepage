// Slice
import { S } from "./style";
// Layer
import { AccountProfile } from "@widgets/accountProfile";

export const UserProfile = () => {

    return (
        <S.userProfile>
            <AccountProfile/>
        </S.userProfile>
    );
}